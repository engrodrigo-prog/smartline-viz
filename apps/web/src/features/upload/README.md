# features/upload

Este namespace agrupará os fluxos de upload (midia, layers, pointcloud) compartilhados entre `/upload/*`. Componentes como gerenciadores de filas, validadores de schema e integrações com a API Hono (`/upload/media/processed`) serão deslocados das páginas conforme evoluirmos.
